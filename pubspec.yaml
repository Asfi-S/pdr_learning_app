name: pdr_learning_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # Бази даних
  sqflite: ^2.3.2
  sqflite_common_ffi: ^2.3.0+4
  path_provider: ^2.1.2

  # Історія тестів (SharedPrefs)
  shared_preferences: ^2.2.2

  # 🔥 Форматування дат (для HistoryScreen)
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true

  assets:
    - assets/pdr/signs/
    - assets/images/
    - assets/pdr/

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/app_icon.png"
  min_sdk_android: 21
